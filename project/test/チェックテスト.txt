testフォルダ内に(自分の名前 ex.izumi),txtを作成し、自分の保存する。

その後、自分のリモートブランチにコミット、プッシュし、リモートが変更になっているかを確認する。

さらに、リクエストプルリクエストをおこない、mainにマージする。

そして、自分のブランチでメインからプルをおこない、最新情報を更新する。
（ここてtestフォルダ内に全員のテキストファイルを生成されているとＯＫ）

また

自分のブランチでprojectのアプリケーションが起動するか確認する。
簡単なアプリが入っているので起動すれば大丈夫。

最後に、開発環境のチェックをしましょう。全員同じであることを確認します。
